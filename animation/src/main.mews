yarn ball main

takes std.graphic
from animation.frames takes frame_keys
from animation.player takes AnimationPlayer
from utils.position takes center

mew frame_rate = 4
mew anim = new AnimationPlayer(frame_keys, frame_rate)

=^.x.^= game_loop()
  graphic.paint("#6e6e6e")

  mew pos = center(32, 32)
  anim.draw(pos.x, pos.y)
meowmeow

graphic.init(game_loop)
