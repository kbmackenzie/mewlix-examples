yarn ball brainfuck.eval

from brainfuck.state takes State

=^.x.^= evaluate(tokens)
  mew state = new State()
  it's raining tokens!! catch a token!!
    token.evaluate(state)
  ~meow
  bring state.get_output()
~meow
